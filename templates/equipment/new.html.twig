{% extends 'base.html.twig' %}

{% block title %}New Equipment{% endblock %}

{% block body %}
<div class="dashboard-container">
    {% include '_partials/sidebar.html.twig' %}

    <main class="dashboard-content">
        <div class="form-card">
            <h1>Create New Equipment</h1>
            
            {{ include('equipment/_form.html.twig', { button_label: 'Save Equipment' }) }}

            <div class="back-link">
                <a href="{{ path('app_equipment_index') }}">← Back to list</a>
            </div>
        </div>
    </main>
</div>
{% endblock %}
