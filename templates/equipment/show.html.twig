{% extends 'base.html.twig' %}

{% block title %}Equipment{% endblock %}

{% block body %}
    <h1>Equipment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment.id }}</td>
            </tr>
            <tr>
                <th>Equipment_Type</th>
                <td>{{ equipment.EquipmentType }}</td>
            </tr>
            <tr>
                <th>Equipment</th>
                <td>{{ equipment.Equipment }}</td>
            </tr>
            <tr>
                <th>Rent_Date</th>
                <td>{{ equipment.RentDate ? equipment.RentDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Payment</th>
                <td>{{ equipment.Payment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipment_index') }}">back to list</a>

    <a href="{{ path('app_equipment_edit', {'id': equipment.id}) }}">edit</a>

    {{ include('equipment/_delete_form.html.twig') }}
{% endblock %}
